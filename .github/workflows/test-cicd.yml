name: CI/CD - Child 02

on:
  # push:
  #   branches: ["main"]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # - uses: actions/checkout@v3

      # - name: Print
      #   run: |
      #     echo Execute child 02 ci/cd pipeline.
      #     echo Testing child 02 ci/cd pipeline.

      - name: Print
        run: echo Hello child 02!
